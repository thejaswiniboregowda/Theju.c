#include<stdio.h>
int main(){
    int a, b, temp;
    printf("enter the value of a and b:\n");
    scanf("%d%d",&a, &b);
    printf("before swapping:a=%d, b=%d,\n", a, b);
    a=temp;
    b=a;
    b=temp;
    printf("after swapping :a=%d, b=%d,\n", a , b);
    return 0;
}
