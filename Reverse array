#include<stdio.h>
int main(){
    int arr[10], n=3;
    printf("enter the elements%d\n", n);
    for(int i=0; i<3; i++){
        scanf("%d", &arr[i]);
    }
    printf("Reverse the array:\n");
    for(int i=3-1; i>=0; i--){
     printf("%d", arr[i]);
    }
}
