#include<stdio.h>
int main(){
    int n, sum=0;
    printf("Enter the element:");
    scanf("%d",&n);
    int arr[n];
    printf("Enter the elements;");
    for(int i=0; i<n; i++){
        scanf("%d",&arr[i]);
        sum=sum+arr[i];
    }
    printf("Sum=%d\n", sum);
    
}
